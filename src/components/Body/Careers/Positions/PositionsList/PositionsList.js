import classes from './PositionsList.module.css';

const PositionsList = () => {
    return(
        <div className={classes.Wrapper}>
            <h6 className={classes.Text}>No positions available at the moment</h6>
        </div>
    )
}

export default PositionsList;