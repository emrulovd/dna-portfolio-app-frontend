import classes from './Positions.module.css';

const Positions = () => {
    return(
        <div className={classes.Wrapper}>
            <h1>Positions</h1>
        </div>
    )
}

export default Positions;